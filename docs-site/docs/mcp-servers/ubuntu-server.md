# Ubuntu Server MCP

Manage an Ubuntu server — file operations, command execution, Docker containers, and systemd services.

## Features

- File read/write/append with path traversal protection
- Command execution with shell injection prevention
- Docker container management
- systemd service status and restart
- System information queries
