# Cloudflare Tunnel

Route traffic securely through Cloudflare Tunnel with zero open ports.

## Setup

1. Install `cloudflared` on your server
2. Authenticate with Cloudflare
3. Create a tunnel for each service port
4. Access services via your Cloudflare domain
