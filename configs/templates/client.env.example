# client.env — MCP credentials for mcp-launcher
# Copy to /DATA/AppData/mcps-server/bin/client.env and fill real values.
# Get these from Web UI: Agents → Add User → Generate → copy snippet.
# MCP_USER_KEY must be PLAINTEXT secret (e.g. K7mX-p3vN-...), NOT sha256$... hash.
MCP_USER_ID=4923472957
MCP_USER_KEY=K7mX-p3vN-change-me
