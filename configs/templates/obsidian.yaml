# Obsidian MCP — Permission Configuration Template
server:
  name: obsidian-mcp
  log_level: INFO
  audit_log: /var/log/mcp/audit.log

ui:
  label: Obsidian
  icon: "📝"
  sections:
    paths: true
    commands: false
    tools: false
    audit: true

connection:
  local:
    vault_path: /data/vaults

permissions:
  default_access: none
  paths: []
  commands: []
  default_command_access: none
